package com.example.demo.entity;

public record Coordinate(int row, int col)
{
//    public Coordinate
//    {
//        if (row < 0 || row > 9 || col < 0 || col > 9) throw new IllegalArgumentException("Wrong coordinates");
//    }
}
